1. have postgres 16 installed on your machine
2. this is easier to use from vscode and from windows (if you use anything else to program I'm sorry)
3. make sure you open this repository as the internally-defined workspace (go to `\DB\CI\preflight\.vscode\DB.code-workspace` and tap `open workspace`)
4. `ctrl+shift+p` should discover a task named `register a new server`. Run it freely to create new servers! yay